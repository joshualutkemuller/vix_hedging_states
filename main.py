"""

Example API Calls to run from Project Root folder where `src/` and `scripts/` live.

# EDA
python -m scripts.run_eda --config config/config.yaml

# States EDA
python -m scripts.run_states_eda --config config/config.yaml

# Backtest
python -m scripts.run_backtest --config config/config.yaml

# Summary
python -m scripts.report_summary --config config/config.yaml

# Grid
python -m scripts.run_grid --config config/config.yaml --k 3 4 5


"""